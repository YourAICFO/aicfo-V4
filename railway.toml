[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "sh -lc 'echo RAILWAY_PROCESS=$RAILWAY_PROCESS; if [ \"$RAILWAY_PROCESS\" = \"worker\" ]; then npm run worker; else npm start; fi'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
